/* -*- Mode: C; c-basic-offset:4 ; indent-tabs-mode:nil ; -*- */
/*
 * See COPYRIGHT in top-level directory.
 */

#include "abti.h"

static ABT_sched_funcs sched_default;
int ABT_sched_default_create_config(ABT_pool pool, ABT_sched_config *newconf);
